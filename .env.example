# Application base URL (used to build verification/reset links)
APP_BASE_URL=http://localhost:3000

# JWT secret (set to a long random string in .env.local)
JWT_SECRET=changeme-super-secret

# SMTP configuration (use your provider or Gmail/Outlook app password)
# --- Gmail (requires 2-Step Verification + App Password) ---
# Generate at: https://myaccount.google.com/apppasswords
#SMTP_HOST=smtp.gmail.com
#SMTP_PORT=465
#SMTP_USER=youraddress@gmail.com
#SMTP_PASS=your_app_password_here
#SMTP_FROM="Job Portal <youraddress@gmail.com>"

# --- Outlook / Microsoft 365 ---
#SMTP_HOST=smtp.office365.com
#SMTP_PORT=587
#SMTP_USER=youraddress@yourdomain.com
#SMTP_PASS=your_app_password_here
#SMTP_FROM="Job Portal <youraddress@yourdomain.com>"

# --- Custom SMTP server ---
#SMTP_HOST=smtp.yourdomain.com
#SMTP_PORT=587
#SMTP_USER=smtp-user
#SMTP_PASS=smtp-password
#SMTP_FROM="Job Portal <no-reply@yourdomain.com>"
